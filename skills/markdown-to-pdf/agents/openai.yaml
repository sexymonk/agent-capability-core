interface:
  display_name: "Markdown to PDF"
  short_description: "Export Markdown docs to PDF"
  default_prompt: "Use $markdown-to-pdf to convert a Markdown document into a clean PDF without browser-added headers or footers."
